const image = document.getElementById('banner');
const heading = document.getElementById('heading')

const toc = document.querySelectorAll('.toclevel-1')
const list = document.querySelector('ul:last-of-type')